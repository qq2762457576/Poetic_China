# 诗意中国 · 仓库审计报告与实施计划

> 审计日期：2026-09-10
> 审计对象：`D:\StudentProject\TotalProject\VScodeProject\git`（GitHub Pages 静态站）
> 线上地址：https://qq2762457576.github.io/Poetic_China/
> 基准文档：《Poetic_China_Product_Level_V2_PRD_AI_Task》
> 审计方式：只读扫描，**未修改任何代码**

---

## 一、结论摘要

**一句话**：项目的内容底座和技术基础比 PRD 预期好得多，但**产品形态缺口很大**——它是「一个做得很精致的诗词数据库」，还**不是** PRD 要求的「阅读 + 学习 + 背诵 + 发现 + 社区产品」。

### 三个最重要的判断

| # | 判断 | 说明 |
|---|---|---|
| 1 | **最紧急的不是加功能，是删假数据** | 站上存在 PRD 点名禁止的伪造数据（`128,450 位诗友`、5 个虚构用户、模拟涨分榜、伪造点赞数）。这是**产品诚信问题**，优先级高于一切新功能 |
| 2 | **「我的」页面完全不存在** | PRD 第五章信息架构的 7 个一级栏目只做了 6 个，缺的恰好是承接「留存」的那个。用户的学习行为**没有落脚点** |
| 3 | **数据架构存在"双轨制"** | 首页统计走真实数据（`FEATURED_STATS`），库页统计硬编码且**已经过时**（写 89,743，实际 89,864）。PRD 第十条要求的 `SITE_STATS` 统一数据源不存在 |

### 基础比预期好的地方（应保留）

- 数据分片懒加载架构设计合理，首屏只载 `featured.js`(54KB)，正文按块懒加载
- XSS 防护扎实：用户内容全部走 `esc()` 转义（45 处），未见注入漏洞
- Design Tokens 已建立（色板/字体/圆角/容器），视觉方向完全符合 PRD 第七条
- SEO 基础齐全（6 页 canonical、sitemap、robots、IndexNow key）
- `notes.js` 449 条注译、`famous_extra` 121 条名篇，内容质量可用

---

## 二、仓库现状

### 2.1 代码规模

| 文件 | 行数 | 说明 |
|---|---|---|
| `assets/js/main.js` | **1,971** | 全部业务逻辑单文件（IIFE），6 页共用 |
| `assets/css/style.css` | 1,403 | 全站样式单文件 |
| `index.html` | 283 | 首页 |
| `library.html` | 258 | 诗词库 |
| `challenge.html` | 252 | 挑战 |
| `community.html` | 211 | 社区 |
| `study.html` | 206 | 课堂/详情 |
| `auth.html` | 119 | 登录注册 |
| `assets/js/cloud.js` | 222 | Supabase 封装 |
| `assets/js/config.js` | 20 | 云端配置 |
| **合计** | **~5,156** | 不含数据文件 |

### 2.2 数据文件

| 文件 | 体积 | 用途 |
|---|---|---|
| `assets/data/poems.js` | **26 MB** | ⚠️ 旧全量数据，**已无页面引用** |
| `assets/data/quizpool.js` | **13 MB** | ⚠️ 旧全量题库，**已无页面引用** |
| `assets/data/poems-index.js` | **11 MB** | ⚠️ 旧单文件索引，**已被分片取代** |
| `assets/data/poems-index/p0..p5.js` | 1.5–2.1 MB × 6 | ✅ 在用（15,000 首/片） |
| `assets/data/poems-text/p0..p29.js` | 0.7–0.9 MB × 30 | ✅ 在用（3,000 首/块） |
| `assets/data/quizpool/p0..p3.js` | 3.1–3.5 MB × 4 | ✅ 在用 |
| `assets/data/notes.js` | 353 KB | ✅ 449 条注译 |
| `assets/data/featured.js` | 54 KB | ✅ 首页精选 + `FEATURED_STATS` |
| `assets/data/banks.js` | 79 KB | ✅ 挑战预解析题库 |
| `assets/data/notes.bak.js` | — | ⚠️ 备份文件，已 gitignore 但仍在磁盘 |

**死重总量约 50 MB**，占仓库数据体积的六成以上。

### 2.3 真实数据规模（已核验）

```js
FEATURED_STATS = { poems: 89864, authors: 5428 }
```
- 诗词：**89,864 首**
- 作者：**5,428 位**
- 注译：449 条（含译文/赏析各 449，创作背景 81）
- 名篇库：121 条

### 2.4 页面依赖关系（已核验）

```
index.html     → featured.js → config.js → cloud.js → main.js
library.html   → index-meta.js → config.js → cloud.js → main.js
study.html     → index-meta.js + notes.js → config.js → cloud.js → main.js
community.html → index-meta.js → config.js → cloud.js → main.js
challenge.html → banks.js → config.js → cloud.js → main.js
auth.html      → config.js → cloud.js → main.js
debug.html     → config.js（诊断工具，noindex）
```
依赖链清晰，无循环依赖。`main.js` 内部按 `document.getElementById` 存在与否判断当前页面，属**隐式路由**。

---

## 三、P0：数据真实性问题（PRD 第十一、四十条）

> PRD 第四十条「禁止」清单第一条即为「伪造用户数据」，第十一条原文点名举例了 `128,450 位诗友`。

### 🔴 违规 1：`auth.html` 伪造用户数

```html
<!-- auth.html:36 -->
<span class="join">加入 128,450 位诗友</span>
```
**这是虚构数字**，且与 PRD 第十一条举的负面例子**逐字一致**。数据库中没有 12.8 万用户。

**修复**：改为 PRD 建议的「加入诗意中国」等不涉及数字的文案。

### 🔴 违规 2：`main.js` 5 个虚构用户 + 模拟涨分榜

```js
// main.js:1687-1691
var RIVALS = [
  { name: '沈砚秋', score: 90 }, { name: '陆栖迟', score: 80 },
  { name: '白也', score: 70 }, { name: '顾清商', score: 60 },
  { name: '江晚吟', score: 50 }
];
// main.js:1712 — 每 20 秒随机一位涨分
setInterval(function () {
  var r = pickOne(RIVALS);
  if (r && r.score < 100 && Math.random() < 0.6) {
    r.score = Math.min(100, r.score + 10);
    renderRank();
  }
}, 20000);
```
**5 个用户不存在**，分数是假的，「更新于 12:33:27」的时间戳也在制造"实时"假象。

**修复**：改为真实榜单（`challenge_records` 表 + RLS 只读聚合），或在后端就绪前**展示空状态**——PRD 第三十四条提供了文案范式。

### 🔴 违规 3：`main.js` 3 条虚构帖子 + 伪造点赞

```js
// main.js:801-804
var SEED_POSTS = [
  { id: 's1', title: '夜读杂兴', author: '沈砚秋', likes: 42, status: 'approved' },
  { id: 's2', title: '念奴娇·昆仑', author: '白也', likes: 38, status: 'approved' },
  { id: 's3', title: '秋日过锦官城', author: '顾清商', likes: 27, status: 'approved' }
];
```
作者名与榜单虚构用户**重合**，点赞数 42/38/27 均为编造。这些是**唯一**能让社区页看起来"有内容"的帖子——移除后会呈现空状态，需一并补齐治理文案。

### 🟡 违规 4：`library.html` 硬编码且过时的统计

```html
<!-- library.html:7  meta description -->
content="89,743 首历代诗词，……"
<!-- library.html:15 og:description -->
content="89,743 首历代诗词，……"
<!-- library.html:78 页面副标题 -->
89,743 首 · 5,409 位诗人，……
```
**数字已过时**（实际 89,864 / 5,428），说明硬编码必然导致数据不一致。

### 🟡 违规 5：首页半硬编码

```html
<!-- index.html:195-198 -->
<span class="stat-num" id="stat-poems">—</span>   <!-- ✅ JS 填充真值 -->
<span class="stat-num" id="stat-authors">—</span> <!-- ✅ JS 填充真值 -->
<span class="stat-num">5</span>                   <!-- ⚠️ 硬编码 -->
<span class="stat-num">3</span>                   <!-- ⚠️ 硬编码 -->
```
后两项（朝代跨度 5、体裁 3）硬编码。

### P0 小结

| 位置 | 问题 | 严重度 |
|---|---|---|
| `auth.html:36` | 伪造 128,450 用户 | 🔴 高 |
| `main.js:1687` | 5 个虚构榜单用户 | 🔴 高 |
| `main.js:1712` | 模拟涨分定时器 | 🔴 高 |
| `main.js:801` | 3 条虚构帖子 + 伪造点赞 | 🔴 高 |
| `library.html:7/15/78` | 硬编码过时统计 | 🟡 中 |
| `index.html:197-198` | 硬编码 5 / 3 | 🟡 中 |

---

## 四、P0：统一数据源缺失（PRD 第十条）

PRD 明确要求建立 `window.SITE_STATS` 并由所有页面读取同一来源。

**现状**：不存在此对象。统计数字有 **4 种来源**：

1. `FEATURED_STATS`（`featured.js`，构建时生成，**真实**）→ 仅首页用
2. 硬编码字符串（`library.html` 三处，**过时**）
3. 运行时统计（`main.js:492-495` 遍历 `loadedPoems()` 数作者，**只统计已载入分片，结果不完整**）
4. 无来源（`auth.html` 的 128,450，纯编造）

**第 3 点是个隐蔽 bug**：首页若 `FEATURED_STATS` 缺失，会退化为遍历已加载诗词数作者——首屏只载 p0（15,000 首），算出的作者数会**严重偏少**。

**修复方向**：构建阶段由 `build_runtime.py` 生成 `assets/data/site-stats.js`：
```js
window.SITE_STATS = { poems: 89864, authors: 5428, dynasties: 5, genres: 3, annotated: 449, featured: 121 };
```
所有页面引用它；`main.js` 统一从它渲染。

---

## 五、P1：核心体验缺口

### 5.1 作品详情页（PRD 第十四条）—— 缺 5 项

| PRD 要求 | 现状 |
|---|---|
| 原文 | ✅ |
| 逐句注释 | ✅ `#panel-notes` |
| 译文 | ✅ `#panel-translation` |
| 创作背景 | ⚠️ **数据存在（81 条 `b` 字段）但无展示入口** |
| 名句 | ❌ 缺失 |
| 作品赏析 | ✅ `#panel-appreciation` |
| 艺术特色 | ❌ 缺失 |
| 作者简介 | ✅ `#poet-card-body` |
| 相关作品 | ✅ `#related-list` |
| 收藏 | ✅ `#fav-btn` |
| 分享 | ❌ 缺失 |
| 开始背诵 | ❌ 缺失 |

**"创作背景"是最容易补的**——数据字段 `b` 已有内容，只是页面没渲染。这是一行代码能解决的 PRD 缺口。

### 5.2 首页（PRD 第六、八条）

**已达成的**：
- 「今日一诗」模块存在（`#daily-poem`）
- Slogan「每天一首，把古人的诗过成自己的日子」已用
- 诗/词/曲三板块存在

**缺口**：
- 首页**缺少「今日读诗」首屏主 CTA**（PRD 6.2 把 `[今日读诗]` 列为第一核心动作）
- 「为你精选」未接**个性化**（无阅读历史驱动）
- **无季节性/节日推荐**（PRD 第八条要求：春季→春日诗词、中秋→月亮思乡、毕业季→送别）
- 「你的学习足迹」模块**不存在**（PRD 6.2 明确要求：已读/已背/收藏/连续天数）

### 5.3 诗词库（PRD 第九条）—— 基础扎实

| 要求 | 现状 |
|---|---|
| 全文/标题/作者/名句搜索 | ✅ |
| 朝代/体裁/主题筛选 | ✅ |
| 排序 / 分页 | ✅ |
| 热门搜索 | ✅ |
| **搜索历史** | ❌ 缺失 |

仅缺 1 项，是全站完成度最高的模块。

### 5.4 挑战（PRD 第十八、十九、二十条）—— 缺 5 项

| 要求 | 现状 |
|---|---|
| 填空 / 上下句 / 背诵 | ✅ |
| 选择题 | ❌ |
| 听诗识诗 | ❌ |
| 作者识别 / 朝代识别 / 主题识别 | ❌ |
| 名句补全 | 部分 |
| **错题系统** | ❌ **完全没有** |
| 学习成就 | ❌ |

**最严重的是错题系统缺失**——PRD 第十九条要求形成闭环：
```
挑战 → 错误 → 解释 → 课堂 → 重新挑战
```
现在答错之后**没有记录、没有回看、没有回到课堂的路径**。这条闭环断裂使「挑战」退化为一次性小游戏，无法沉淀学习。

### 5.5 课堂（PRD 第十七条）—— 缺 4 项

| 要求 | 现状 |
|---|---|
| 逐句学习 | ✅ |
| 学习进度 | ✅ |
| 精品课程 | ❌ |
| 主题学习 | ❌ |
| 作者专题 | ❌ |
| 朝代专题 | ❌ |
| **六步学习流程** | ❌ 缺失 |

PRD 第十七条明确要求六步：读原文 → 逐句理解 → 了解背景 → 理解名句 → 整体赏析 → 开始背诵，并显示 `4 / 6` 进度。**现状是详情页的展示，不是"学习过程"**——这正是 PRD 开头强调的「课堂不是作品详情的简单复制」。

---

## 六、P1：结构性缺口

### 🔴 「我的」页面完全不存在

PRD 第五章信息架构：
```
首页 / 诗词库 / 作品详情 / 课堂 / 挑战 / 社区 / 我的
                                              ↑ 缺失
```
PRD 第二十二条要求的内容**全部无处承载**：

| 要求 | 现状 |
|---|---|
| 你好，XXX | ❌ |
| 今日学习 / 连续学习 X 天 | ❌ |
| 已阅读 / 已背诵 / 收藏 数 | ❌ 数据散落 localStorage，无汇总视图 |
| 最近阅读 | ❌ |
| 我的收藏 | ❌ |
| 我的错题 | ❌ |
| 我的成就 | ❌ |
| 学习趋势可视化 | ❌ |

**这是留存链路的断点**。用户读诗、打卡、收藏产生的数据**没有任何一个页面能看到全貌**。PRD 的核心闭环「内容 → 学习 → 成就 → 社交 → 留存」在最后一环断裂。

### 🔴 移动端底部导航不完整

PRD 第二十七条要求 5 项：
```
首页 / 诗词库 / 挑战 / 课堂 / 我的
```
CSS 中有 `--tabbar-h: 62px` 且 `index.html` 有 2 处 tabbar 引用，但**「我的」无对应页面**。

### 🟡 精品内容分层未落地（PRD 第十五条）

PRD 要求「海量基础库 + 精品内容层」：
- 第一阶段 50–100 首精品 → **已达成**（`notes.js` 449 条，`famous_extra` 121 条）
- 但**没有"精品"的视觉标识与筛选入口**——用户无法区分哪些诗有深度内容

---

## 七、P2/P3：用户系统与安全

### 7.1 用户系统现状

| PRD 第十二条要求 | 现状 |
|---|---|
| 注册 / 登录 / 登出 | ✅ |
| 邮箱验证 | ⚠️ Supabase 支持，但**未开启**（SETUP.md 让用户手动关） |
| 密码重置 | ❌ |
| 用户头像 | ❌ |
| 用户昵称 | ✅ |
| 收藏/历史/进度/成绩同步 | ❌ **仅存 localStorage** |
| 跨设备同步 | ❌ |

### 7.2 安全评估

**做得好的**：
- 云端密码走 Supabase Auth（bcrypt 服务端哈希）
- 本地模式 SHA-256 加盐（`crypto.subtle`）
- RLS 策略存在
- XSS 防护到位（`esc()` 45 处覆盖用户内容）

**待改进**：
| PRD 第十三条要求 | 现状 |
|---|---|
| 密码不明文 | ✅ |
| 服务端哈希 | ✅（云端模式） |
| 防 XSS | ✅ |
| **防 CSRF** | ⚠️ 未见显式措施 |
| **登录限流/防暴破** | ❌ 无 |
| 会话清理 | ✅（`signOut`） |

**本地模式的安全定位需明确**：PRD 第十三条强调「localStorage 中的演示账号逻辑不能直接视为生产级安全方案」。当前站点**在未配置云端时静默降级为本地模式**，普通用户无法感知。建议在 UI 上明示模式。

### 7.4 Supabase 云端实测（2026-09-10 12:50 核实）

> 用户已配置 Supabase。以下是**实测**结果（非推测）。

**项目信息**
| 项 | 值 |
|---|---|
| Project URL | `https://ocdomwjlipcwzfvzbowy.supabase.co` |
| ref | `ocdomwjlipcwzfvzbowy` |
| config.js 本地/线上 | ✅ 一致 |
| 端点延迟 | ~0.66s（成都直连） |

**表结构（实测，字段可用）**
| 表 | 字段 | 数据 |
|---|---|---|
| `posts` | id, user_id, author, kind, title, body, poem_id, status, likes, created_at | 空（0 行） |
| `comments` | id, post_id, user_id, author, body, created_at | 空（0 行） |

**安全策略（实测通过）**
- 匿名读取 posts → `HTTP 200`（公开读生效）
- 匿名写入 posts → `HTTP 401` + `42501 new row violates row-level security policy`（**RLS 正确拦截**）
- `increment_likes(post_id)` → `HTTP 204`（RPC 正常）

**⚠️ 发现两个待处理项**

1. **邮箱验证仍开启**（`mailer_autoconfirm: false`）
   SETUP.md 第 4 节说明要关，但**用户尚未执行**。实测注册返回：
   ```
   HTTP 429  over_email_send_rate_limit
   ```
   Supabase 免费项目内置 SMTP **每小时仅 3 封**。含义：**第 3 个之后的注册用户当天无法注册**，且邮件极易进垃圾箱。
   → 必须先关 `Authentication → Sign In / Providers → Email → Confirm email`，或自配 SMTP。

2. **`config.js` 注释未清理**
   ```js
   supabaseAnonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9jZG9td2psaXBjd3pmdnpib3d5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODg5OTQzNzEsImV4cCI6MjEwNDU3MDM3MX0.uedhjsz8FB_imcrnkLVzU6GA9iHPGLnZydjPtc29jjg'   // ← 待填：anon / publishable 密钥
   ```
   密钥已填，但遗留「← 待填」注释，易误导（本地与线上均如此）。

**对实施计划的影响**：Phase 2「云端同步」的**基础设施已就绪**，无需再建表/配 RLS。可直接开始写同步逻辑，只需先关邮箱验证。

### 7.3 合规页面缺失（PRD 第三十二、三十三条）

**以下页面全部不存在**：
- 隐私政策
- 用户协议
- Cookie / 本地存储说明
- 数据来源与版权说明（PRD 第三十二条专门强调要区分：第三方数据 / 数据许可 / 人工编校 / 原创 / 用户投稿 / AI 辅助）

页脚目前仅有「2026 诗意中国 · 诗词原文源自公开古籍与开源数据集」一句笼统说明——**这正是 PRD 第三十二条反对的"笼统描述"**。

---

## 八、P2：工程质量与技术债

| 项 | 现状 | 影响 |
|---|---|---|
| **单文件巨石** | `main.js` 1,971 行承载全部逻辑 | 可维护性差，改动风险高 |
| **无组件化** | PRD 第三十八条列出的 15 个组件全无抽象 | `postCard`/`poemCard` 等虽有函数，但 Header/Footer/Nav **在 6 个 HTML 中重复** |
| **`data-page-node-id` 残留** | **873 处**（index 185、library 178、challenge 151、study 131、community 130、auth 61、debug 37） | 设计稿导出残留，污染 DOM，含 `data-` 属性语义错误 |
| **隐式路由** | 按 DOM 存在性判断页面 | 无显式页面标识，易出错 |
| **死重数据 50MB** | `poems.js` 26M + `quizpool.js` 13M + `poems-index.js` 11M | 仓库臃肿，克隆慢 |
| **无构建流程** | 纯手工，无 lint/format/测试（仅 `test_runtime.js`） | 质量无自动化保障 |
| **无错误监控** | PRD 第四十五条要求「错误监控」 | 线上问题不可见 |
| **无 skeleton** | PRD 第三十五条要求，全站 0 处 | 加载时白屏/文字跳变 |

### 无障碍（PRD 第二十八条）—— 明显不足

| 要求 | 现状 |
|---|---|
| **`:focus-visible`** | ❌ **0 处**（PRD 提供了具体代码） |
| `role=` | ❌ 0 处 |
| `alt` | ⚠️ 仅 1 处 |
| `aria-label` | ✅ 13 处 |
| `prefers-reduced-motion` | ✅ 1 处 |

键盘可见焦点缺失是**明确的用户伤害**——键盘用户无法知道自己聚焦在哪里。

### SEO（PRD 第三十条）

| 要求 | 现状 |
|---|---|
| title / description / canonical | ✅ 6/6 |
| Open Graph | ⚠️ 有 og:title/description，**缺 og:image** |
| Twitter Card | ❌ 0/6 |
| **structured data** | ⚠️ 仅 1 页有 json-ld |
| sitemap.xml | ⚠️ 仅 5 条 |
| robots.txt | ✅ |

**最大机会**：PRD 第三十条要求「重点作品应该可以被搜索引擎独立索引」，例如
`《静夜思》李白_原文、注释、译文与赏析｜诗意中国`。
当前 `study.html?id=89743` 是**单一模板页**，搜索引擎无法为 89,864 首作品分别建索引——这是**最大的一块 SEO 未开发流量**。

---

## 九、PRD 逐条差距总表

| PRD 章节 | 要求 | 状态 | 优先级 |
|---|---|---|---|
| 十 | 统一数据源 SITE_STATS | ❌ 不存在 | **P0** |
| 十一 | 禁止虚假数据 | ❌ 5 处违规 | **P0** |
| 十四 | 作品详情页 11 项 | ⚠️ 缺 5 项 | P1 |
| 六/八 | 首页今日读诗 + 个性化 | ⚠️ 部分 | P1 |
| 九 | 诗词库 | ✅ 仅缺搜索历史 | P2 |
| 十七 | 课堂六步流程 | ❌ 缺 4 项 | P2 |
| 十八/十九/二十 | 挑战玩法 + 错题闭环 | ❌ 缺 5 项 | P1 |
| 二十一 | 学习成就 | ❌ 缺失 | P2 |
| 二十二 | 个人中心「我的」 | ❌ **整页缺失** | **P1** |
| 二十三/二十四 | 社区 + 治理 | ⚠️ 有基础，缺举报/管理后台 | P3 |
| 二十五 | 推荐系统 | ❌ 缺失 | P3 |
| 二十六 | AI 功能 | ❌ 缺失 | P5 |
| 二十七 | 移动端 | ⚠️ 底部导航缺「我的」 | P1 |
| 二十八 | 无障碍 | ⚠️ 缺 focus-visible/role/alt | P1 |
| 二十九 | 性能 | ✅ 分片架构良好 | — |
| 三十 | SEO | ⚠️ 缺 og:image/twitter/作品页索引 | P1 |
| 三十一 | PWA | ❌ 未做 | P6 |
| 三十二 | 数据来源说明 | ❌ 缺失 | P1 |
| 三十三 | 隐私政策/用户协议 | ❌ 缺失 | P1 |
| 三十四 | 空状态 | ⚠️ 有 `empty-state` 但文案未对齐 PRD | P2 |
| 三十五 | 加载状态/skeleton | ❌ 无 skeleton | P2 |
| 三十七 | Design Tokens | ✅ 基础已有 | — |
| 三十八 | 组件化 | ❌ 缺失 | P2 |
| 三十九 | 代码质量 | ⚠️ main.js 巨石 | P2 |
| 四十 | 禁止事项 | ❌ 违反 1/2/3 条 | **P0** |

---

## 十、实施计划

> 原则（PRD 第三十九、四十六条）：**小步修改 → 测试 → 修复 → 再修改**。不重写整个项目。

### Phase 0：数据诚信 + 架构基础（P0）

**目标**：让站上每一个数字都真实可信。这是产品底线。

| # | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| 0.1 | 删除 `auth.html` 的「加入 128,450 位诗友」 | `auth.html` | 改为「加入诗意中国」，无数字 |
| 0.2 | 移除 `RIVALS` 虚构榜单 + 涨分定时器 | `main.js:1683-1718` | 无虚构用户，改空状态或真实榜 |
| 0.3 | 移除 `SEED_POSTS` 虚构帖子 | `main.js:801-804, 849` | 社区无内容时展示 PRD 空状态文案 |
| 0.4 | 建立 `site-stats.js` 统一数据源 | 新增 + `build_runtime.py` | `window.SITE_STATS` 存在 |
| 0.5 | 全站统计改读 `SITE_STATS` | `library.html`、`index.html`、`main.js` | 无硬编码数字，各页数值一致 |
| 0.6 | 清理 50MB 死重数据 | 删 `poems.js`/`quizpool.js`/`poems-index.js` | 仓库体积下降，功能无回归 |
| 0.7 | 清理 873 处 `data-page-node-id` | 6 个 HTML | DOM 干净 |

**验证**：全站搜索 `[0-9]{1,3}(,[0-9]{3})` 无硬编码统计；各页数字一致且等于 89,864。

---

### Phase 1：核心体验（P1）

**目标**：补齐详情页与首页，让「读一首诗」的体验完整。

| # | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| 1.1 | 详情页补「创作背景」（数据已有，只缺渲染） | `study.html`、`main.js` | 81 条有 `b` 的诗显示背景 |
| 1.2 | 详情页补「分享」 | `study.html`、`main.js` | Web Share API + 复制链接兜底 |
| 1.3 | 详情页补「开始背诵」入口 | `study.html`、`main.js` | 跳转挑战背诵模式 |
| 1.4 | 详情页补「名句」高亮区块 | `study.html`、`main.js` | 名句可独立展示 |
| 1.5 | 首页补「今日读诗」首屏 CTA | `index.html` | 首屏唯一主按钮 |
| 1.6 | 首页补「你的学习足迹」模块 | `index.html`、`main.js` | 已读/已背/收藏/连续天数 |
| 1.7 | 季节性推荐（春/中秋/毕业季） | `main.js` | 按时令返回对应主题 |
| 1.8 | 无障碍基础：`:focus-visible` | `style.css` | PRD 给定代码生效 |
| 1.9 | SEO：补 og:image + twitter:card | 6 个 HTML | 社交分享有预览图 |
| 1.10 | 数据来源与版权说明页 | 新增 `sources.html` | PRD 六类来源分别说明 |

---

### Phase 2：「我的」页面 + 用户系统（P1）

> **前提已满足**：Supabase 已配置，posts/comments 表与 RLS 实测通过（见 7.4）。无需再建表。

**目标**：补上留存落脚点。

| # | 任务 | 验收 |
|---|---|---|
| 2.0 | **关掉邮箱验证**（或自配 SMTP） | 注册后立即可登录，无 429 风险 |
| 2.1 | 新建 `me.html` | 7 项内容全部呈现 |
| 2.2 | 学习足迹数据汇总 | localStorage 聚合成视图 |
| 2.3 | 学习趋势可视化 | 简图展示近期趋势 |
| 2.4 | 移动端底部导航补「我的」 | 5 项齐全 |
| 2.5 | 收藏 / 历史 / 进度云端同步（新建 user_data 表） | 登录后跨设备一致 |
| 2.6 | 密码重置 | 邮件重置可用 |
| 2.7 | 用户头像 | 可上传/选默认 |
| 2.8 | 明示账号模式（云端/本地） | 用户可感知 |

---

### Phase 3：学习系统（P2）

| # | 任务 | 验收 |
|---|---|---|
| 3.1 | 课堂六步学习流程 | 显示 `X / 6` 进度 |
| 3.2 | 错题系统（记录/列表/再练/掌握） | PRD 第二十条全项 |
| 3.3 | 挑战 → 错题 → 课堂闭环 | 答错可直达课堂 |
| 3.4 | 挑战反馈文案（妙哉/差一点） | PRD 第十九条 |
| 3.5 | 学习成就（6 项） | PRD 第二十一条 |
| 3.6 | 连续学习天数 | 真实计算 |
| 3.7 | 课堂专题（主题/作者/朝代） | 三个入口 |

---

### Phase 4：社区（P3）

| # | 任务 | 验收 |
|---|---|---|
| 4.1 | 举报功能 | 可举报 |
| 4.2 | 内容治理（删除/屏蔽） | 有处置路径 |
| 4.3 | 管理后台 | 审核队列 |
| 4.4 | 帖子关联作品 | 点击直达详情页 |
| 4.5 | 敏感内容处理 | 有策略 |

---

### Phase 5：工程质量（P2，可与 1–4 并行）

| # | 任务 | 验收 |
|---|---|---|
| 5.1 | `main.js` 按模块拆分 | 单文件 < 500 行 |
| 5.2 | Header/Footer/Nav 组件化 | 消除 6 处重复 |
| 5.3 | skeleton 加载态 | 无白屏 |
| 5.4 | 空状态文案对齐 PRD 第三十四条 | 5 类空状态 |
| 5.5 | 隐私政策 / 用户协议 | 独立页面 |
| 5.6 | 错误监控接入 | 线上异常可见 |
| 5.7 | Design Tokens 补全（shadow/间距分级） | PRD 第三十七条 |
| 5.8 | 作品页 SEO 化（重点作品可独立索引） | 精品页有独立 URL/结构化数据 |

---

### Phase 6：智能化与规模化（P5–P6）

AI 解释 / AI 出题 / AI 背诵 / AI 推荐 / PWA / CDN / 推荐系统 / 数据分析。

---

## 十一、建议的执行顺序

```
立即（本周）
  ├─ Phase 0 全部 ← 诚信问题，不能拖
  ├─ 1.1 / 1.8 / 1.9 ← 成本极低，收益明显
  └─ 2.0 关邮箱验证 ← 5 分钟，不关注册功能等于半残

随后
  ├─ Phase 1 剩余
  └─ Phase 2「我的」← 留存断点

中期
  ├─ Phase 3 学习系统
  └─ Phase 5 工程质量

长期
  └─ Phase 4 社区 → Phase 6 规模化
```

### 云端就绪带来的两个变化

1. **Phase 2 可以提前**：基础设施已通（表 + RLS + RPC 实测通过），不必等 Phase 1 全部完成就能开工。**先做「我的」页面**收益最大——它同时承接了 Phase 1 的学习足迹和 Phase 3 的错题/成就。
2. **Phase 2.0（关邮箱验证）应提到最前面**：这是当前唯一会**直接阻断真实用户注册**的问题，且修复只需点一下开关。

调整后的建议顺序：
```
① Phase 0（数据诚信）
② 2.0 关邮箱验证
③ 1.1 详情页创作背景 / 1.8 focus-visible
④ 2.1-2.4「我的」页面 + 移动端导航
⑤ Phase 1 剩余 → Phase 3 学习系统
```

### 三个"投入产出比最高"的动作

1. **详情页补「创作背景」** — 数据已有（81 条 `b` 字段），只需渲染，**一行代码级别的成本**，补掉 PRD 第十四条的一个缺口
2. **`:focus-visible`** — PRD 直接把代码写好了，**复制即用**，解决真实的无障碍伤害
3. **清理 `data-page-node-id`** — 机械操作，873 处一次清完，DOM 立刻干净

### 一个需要你决策的点

**本地模式是否保留？**

当前站点在未配置 Supabase 时**静默降级为 localStorage 模式**，账号、收藏、进度都存在本机。这带来两个问题：

- 用户以为"注册了"，换设备却什么都没有
- PRD 第十三条明确说 localStorage 不能当生产级安全方案

**现在云端已配置**，这个问题实质上已经解决——站点会自动走云端模式。但仍有两点要处理：

- **降级路径仍在**：若 Supabase 临时不可用，`cloud.js` 会静默回落本地，用户无法察觉数据没上云
- **建议**：在 `me.html`（Phase 2.8）明确显示「云端同步已开启 / 仅存本机」

结论：**保持双模式，但必须让用户看得见当前处于哪个模式**。

---

## 附录：审计方法说明

本次审计为**只读扫描**，未修改任何代码。核验手段：

- 文件规模：`wc -l` / `du -h`
- 依赖关系：提取各 HTML 的 `<script src>` 并去重
- 硬编码检测：正则 `[0-9]{1,3}(,[0-9]{3})+` 全文匹配
- 功能对照：按 PRD 章节逐项 grep 关键词确认存在性
- 数据真实性：比对 `FEATURED_STATS`（构建时生成）与 HTML 硬编码值

**需注意的审计局限**：
- 功能存在性通过关键词匹配判断，**可能漏判**（如某功能用不同命名实现）
- 未做浏览器实测与性能跑分
- 未审计 Supabase 线上的实际 RLS 策略与数据
