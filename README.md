# git

练习用的 Git 仓库。

```bash
git clone <仓库地址>
cd git
```

## 常用命令备忘

```bash
git status              # 查看当前状态
git add .               # 暂存所有改动
git commit -m "说明"     # 提交
git push                # 推送到远程
git pull                # 拉取远程更新
git log --oneline       # 查看提交历史
```
